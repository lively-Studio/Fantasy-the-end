// Fantasy: The End —— 多版本构建根工程（容器）。
// 实际模组构建位于 versions/fabric-1.21.x 与 versions/fabric-26.x。
// 共享源码放在 common/，由两个版本工程一起编译。
//
// 各版本独立出 jar：
//   ./gradlew :fabric-1.21.x:build
//   ./gradlew :fabric-26.x:build
